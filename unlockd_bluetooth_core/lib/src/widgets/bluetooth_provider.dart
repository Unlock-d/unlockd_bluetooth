import 'package:flutter/material.dart';

class BluetoothProvider extends StatelessWidget {
  const BluetoothProvider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
